export const TabContent = ({children}) => {

    return (
        <div className="tab-content-component tab-pane fade show active">
            <div className="p-2">{children}</div>
        </div>
    )

    
}